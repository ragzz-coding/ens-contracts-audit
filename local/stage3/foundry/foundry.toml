[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200
via_ir = false
ffi = false
evm_version = "cancun"
fs_permissions = [{ access = "read", path = "./"}]

[rpc_endpoints]
# intentionally empty — tests use local anvil / in-process cheatcodes only
